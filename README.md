# todo-desktop-app
Spring Boot TODO Backend This project is a RESTful task management API built with Spring Boot and PostgreSQL. It uses a layered architecture (Controller → Service → Repository) and JPA/Hibernate for data persistence. Features:  CRUD operations via REST endpoints  Soft delete using @SQLDelete and @Where annotations 
